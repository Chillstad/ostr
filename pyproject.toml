[project]
name = "ostr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "importlib>=1.0.4",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = [
  ".",
  "./commands/"
]
